<form method="post" action="{{ path('app_annee_universitaire_delete', {'id': annee_universitaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annee_universitaire.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
