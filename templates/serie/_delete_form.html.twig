<form method="post" action="{{ path('app_serie_delete', {'id': serie.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet enregistrement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
