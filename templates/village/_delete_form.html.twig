<form method="post" action="{{ path('app_village_delete', {'id': village.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet enregistrement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ village.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
